export function confirmAction(message:string):Promise<boolean>{return new Promise(resolve=>{const dialog=document.createElement('dialog');dialog.className='app-confirm';const title=document.createElement('h2');title.textContent='تأكيد الإجراء';const text=document.createElement('p');text.textContent=message;const actions=document.createElement('div');const yes=document.createElement('button'),no=document.createElement('button');yes.textContent='تأكيد';no.textContent='إلغاء';yes.className='confirm-primary';const finish=(ok:boolean)=>{dialog.close();dialog.remove();resolve(ok)};yes.onclick=()=>finish(true);no.onclick=()=>finish(false);dialog.oncancel=e=>{e.preventDefault();finish(false)};actions.append(no,yes);dialog.append(title,text,actions);document.body.append(dialog);dialog.showModal();no.focus()})}
